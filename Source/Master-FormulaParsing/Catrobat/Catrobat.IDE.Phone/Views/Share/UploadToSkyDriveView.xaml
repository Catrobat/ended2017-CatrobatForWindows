<phone:PhoneApplicationPage 
  x:Class="Catrobat.IDE.Phone.Views.Share.UploadToSkyDriveView"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
  xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:controls="clr-namespace:Microsoft.Live.Controls;assembly=Microsoft.Live.Controls"
  FontFamily="{StaticResource PhoneFontFamilyNormal}"
  FontSize="{StaticResource PhoneFontSizeNormal}"
  Foreground="{StaticResource PhoneForegroundBrush}"
  SupportedOrientations="Portrait" Orientation="Portrait"
  mc:Ignorable="d" d:DesignHeight="768" d:DesignWidth="480"
  DataContext="{Binding UploadToSkyDriveViewModel, Source={StaticResource Locator}}"
  shell:SystemTray.IsVisible="True">

  <Grid x:Name="LayoutRoot" Background="Transparent">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <!--<StackPanel Grid.Row="0" Margin="10 0 0 20">
      <TextBlock Margin="0" Text="{Binding Resources.Main_TitleSettings, Source={StaticResource LocalizedStrings}}" Style="{StaticResource PhoneTextNormalStyle}" />
      <TextBlock Margin="0 -10 0 0" Text="{Binding Resources.Main_HeaderSettingsBricks, Source={StaticResource LocalizedStrings}}" Style="{StaticResource PhoneTextTitle1Style}"/>
    </StackPanel>-->

    <Grid Grid.Row="1" Margin="10 0 0 15">
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition />
      </Grid.RowDefinitions>

      <StackPanel Grid.Row="0" Margin="10 20 0 15">
        <controls:SignInButton ClientId="000000004C102831" 
                               Scopes="wl.signin wl.basic wl.skydrive wl.skydrive_update"
                               Branding="Skydrive"
                               TextType="SignIn"
                               SessionChanged="SignInButton_OnSessionChanged" 
                               HorizontalAlignment="Center"  
                               VerticalAlignment="Center" />

        <Grid Visibility="{Binding IsUploading, Converter={StaticResource BoolVisibilityConverter}}">
          <ProgressBar Margin="0 50 0 0" IsIndeterminate="true" />
          <TextBlock Text="Wait until the project has finished uploading!"/>
        </Grid>
        

      </StackPanel>
    </Grid>
  </Grid>
</phone:PhoneApplicationPage>
