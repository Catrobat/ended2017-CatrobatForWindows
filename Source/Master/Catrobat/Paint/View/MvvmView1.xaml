<UserControl x:Class="Catrobat.Paint.View.MvvmView1"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:ignore="http://www.ignore.com" xmlns:vm="clr-namespace:Catrobat.Paint.ViewModel"
             mc:Ignorable="d ignore"
             >

    <UserControl.Resources>
        <ResourceDictionary>
            <vm:ViewModelLocator x:Key="Locator" />
        </ResourceDictionary>
    </UserControl.Resources>


    <UserControl.DataContext>
        <Binding Source="{StaticResource Locator}" Path="Main" />
    </UserControl.DataContext>


    <Grid>


        <Button Grid.Row="1"
                Command="{Binding IncrementValue}"
                Content="Increment value" HorizontalAlignment="Center" VerticalAlignment="Center" Opacity="0.7" />
        <TextBlock HorizontalAlignment="Left" Margin="221,486,0,0" TextWrapping="Wrap"  Text="{Binding ExampleValue}" VerticalAlignment="Top" Height="67"/>

    </Grid>
</UserControl>
