<UserControl x:Class="Catrobat.IDE.Store.Views.Editor.Sounds.NewSoundSourceSelectionView"
  x:Name="PageRoot"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:misc="using:Catrobat.IDE.Store.Content.Images.Misc"
  mc:Ignorable="d"
  DataContext="{Binding NewSoundSourceSelectionViewModel, Source={StaticResource Locator}}"
  d:DesignHeight="300" d:DesignWidth="520">

  <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Grid>
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="Auto"/>
      </Grid.ColumnDefinitions>
      <Grid>
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!--<StackPanel x:Name="TitlePanel" Grid.Row="0" Margin="10 0 10 20">
          <TextBlock Margin="10" Style="{StaticResource SubheaderTextBlockStyle}"
                     Text="{Binding Resources.Editor_TitleNewSound, Source={StaticResource LocalizedStrings}}" />
        </StackPanel>-->

        <StackPanel Grid.Row="1" Margin="0 0 0 0" HorizontalAlignment="Stretch">
          <Button Margin="0 0 0 0" HorizontalAlignment="Stretch" Command="{Binding RecorderCommand}">
            <Button.Template>
              <ControlTemplate>
                <Grid Background="{Binding Path=SelectedTheme.AccentColor1.NativeBrush, Source={StaticResource ThemeChooser}}" 
                    HorizontalAlignment="Stretch">
                  <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition/>
                  </Grid.ColumnDefinitions>
                  <misc:AudioLibrary ScaleFactor="0.10" Margin="30 15 15 15"></misc:AudioLibrary>
                  <TextBlock Grid.Column="1" Margin="20 10 50 10" VerticalAlignment="Center" HorizontalAlignment="Left" 
                           Text="{Binding Resources.Editor_MediaLibrary, Source={StaticResource LocalizedStrings}}" 
                           Name="tbGallery" Style="{StaticResource SubheaderTextBlockStyle}" Foreground="White"/>
                </Grid>
              </ControlTemplate>
            </Button.Template>
          </Button>

          <Button Margin="0 5 0 0" HorizontalAlignment="Stretch" Command="{Binding RecorderCommand}">
            <Button.Template>
              <ControlTemplate>
                <Grid Background="{Binding Path=SelectedTheme.AccentColor2.NativeBrush, Source={StaticResource ThemeChooser}}" 
                    HorizontalAlignment="Stretch">
                  <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition/>
                  </Grid.ColumnDefinitions>
                  <misc:AudioRecorder ScaleFactor="0.10" Margin="30 15 15 15"></misc:AudioRecorder>
                  <TextBlock Grid.Column="1" Margin="20 10 50 10" VerticalAlignment="Center" HorizontalAlignment="Left" 
                           Text="{Binding Resources.Editor_Recorded, Source={StaticResource LocalizedStrings}}" 
                           Style="{StaticResource SubheaderTextBlockStyle}" Foreground="White" />
                </Grid>
              </ControlTemplate>
            </Button.Template>
          </Button>
        </StackPanel>
      </Grid>
    </Grid>
  </Grid>
</UserControl>
