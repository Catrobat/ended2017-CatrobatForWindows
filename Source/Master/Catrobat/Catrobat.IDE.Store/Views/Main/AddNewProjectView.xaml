<UserControl x:Class="Catrobat.IDE.Store.Views.Main.AddNewProjectView"
  x:Name="PageRoot"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  mc:Ignorable="d"
  DataContext="{Binding AddNewProjectViewModel, Source={StaticResource Locator}}"
  d:DesignHeight="150" d:DesignWidth="500">

  <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Grid>
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="Auto"/>
      </Grid.ColumnDefinitions>
      <Grid>
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!--<StackPanel x:Name="TitlePanel" Grid.Row="0" Margin="10 0 10 20">
          <TextBlock Margin="10" Style="{StaticResource SubheaderTextBlockStyle}"
                     Text="{Binding Resources.Main_TitleNewProject, Source={StaticResource LocalizedStrings}}" />
        </StackPanel>-->

        <Grid Grid.Row="1" Margin="10 0 0 15" HorizontalAlignment="Stretch">
          <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
          </Grid.RowDefinitions>

          <TextBlock Grid.Row="0" Margin="10" Style="{StaticResource TitleTextBlockStyle}"
                     Text="{Binding Resources.Editor_ProjectName, Source={StaticResource LocalizedStrings}}" />
          
          <TextBox Grid.Row="1" Margin="10" Name="TextBoxProjectName"
                   Text="{Binding ProjectName, Mode=TwoWay}" 
                 HorizontalAlignment="Stretch" TextChanged="TextBoxProjectName_OnTextChanged" />
          
          <CheckBox Grid.Row="2" Margin="0 15 0 0" IsChecked="{Binding CopyCurrentProjectAsTemplate, Mode=TwoWay}" >
            <TextBlock TextWrapping="Wrap" Text="{Binding TextCopyCurrentProjectAsTemplate }"></TextBlock>
          </CheckBox>
        </Grid>
      </Grid>
      <Grid Grid.Column="1">
        <AppBarButton VerticalAlignment="Bottom" Icon="Save" 
                      Label="{Binding Resources.Editor_ButtonSave, Source={StaticResource LocalizedStrings}}" 
                      Command="{Binding SaveCommand}"/>
      </Grid>
    </Grid>
  </Grid>
</UserControl>
